[build]
  base    = "."
  command = "pnpm --filter @workspace/trashboy run build"
  publish = "artifacts/trashboy/dist/public"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
