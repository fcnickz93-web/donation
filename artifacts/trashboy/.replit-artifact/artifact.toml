kind = "web"
previewPath = "/"
title = "Trashboy Donations"
version = "1.0.0"
id = "artifacts/trashboy"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21640

[services.development]
run = "pnpm --filter @workspace/trashboy run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trashboy", "run", "build" ]
publicDir = "artifacts/trashboy/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21640"
BASE_PATH = "/"
